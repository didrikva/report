{% extends 'base.html.twig' %}

{% block title %}Hello LibraryController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <form method="post" action="{{ path('library_update_post', { id: library.id }) }}">
        <label for="titel">Titel:</label>
        <input type="text" name="titel" value="{{ library.titel }}">
        <label for="isbn">ISBN:</label>
        <input type="text" name="isbn" value={{ library.isbn }}>
        <label for="forfattare">Författare:</label>
        <input type="text" name="forfattare" value="{{ library.forfattare }}">
        <label for="img">Bild: (optinal)</label>
        <input type="text" name="img" value="{{ library.img }}">

        <input type="submit" value="Spara bok">
    </form>

</div>
{% endblock %}
