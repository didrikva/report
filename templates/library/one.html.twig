{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <table class="library-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Titel</th>
                <th>ISBN</th>
                <th>Författare</th>
                <th>Bild</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ library.id }}</td>
                <td>{{ library.titel }}</td>
                <td>{{ library.isbn }}</td>
                <td>{{ library.forfattare }}</td>
                <td><img src="{{ asset(library.getImg) }}" alt="{{ library.getTitel }}" style="height: 80px;" /></td>
            </tr>
        </tbody>
    </table>
     <a href="{{ path('library_update_get', { id: library.getId }) }}"><button class ="update">Uppdatera bok</button></a>
     <a href="{{ path('library_delete_by_id', { id: library.getId }) }}"><button class ="delete">Radera bok</button></a>
</div>
<a href="{{ path('library_view_all') }}"><button class ="tillbaka">Tillbaka</button></a>
{% endblock %}
